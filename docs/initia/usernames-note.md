# .init Username Integration

The leaderboard and agent pages resolve Initia `.init` usernames via the Initia Names API.
A screenshot (`docs/initia/usernames.png`) should be captured from a live testnet session
showing a `.init` username in the leaderboard owner column.

Fallback: accounts without a `.init` name continue to show `0xabcd…1234` shortened hex.
