{
  "name": "playerco",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "description": "PlayerCo - Poker Agent Company on-chain",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  },
  "pnpm": {
    "overrides": {
      "next": "14.2.35"
    }
  }
}
