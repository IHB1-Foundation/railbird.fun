{
  "name": "@playerco/contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "coverage": "forge coverage --ir-minimum",
    "coverage:lcov": "forge coverage --ir-minimum --report lcov",
    "clean": "forge clean",
    "lint": "solhint 'src/**/*.sol'"
  },
  "devDependencies": {
    "solhint": "^6.2.1"
  }
}
